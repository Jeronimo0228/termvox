# Release readiness

This repository is prepared as `0.1.0-alpha.1`. Nothing has been published.

## Local gates completed

- `cargo fmt --all`
- Clippy with warnings denied, all targets/features
- Workspace tests with native Linux audio enabled
- Portable no-default-feature tests
- Rust API documentation build
- Package archive dry-run for the workspace
- Verified package builds for the independent base crates
- CLI smoke tests for JSON doctor output, Bash completion, and manpage generation
- `cargo audit` and `cargo deny check`
- mdBook HTML build
- Native Linux microphone discovery and one-second capture
- CycloneDX 1.5 SBOM generation and validation
- One official Spanish Vosk model independently downloaded and checksummed

## Artifact inventory

- Seven Rust packages and the `termvox` binary
- Shell and PowerShell installation/release preparation
- Bash/Zsh/Fish/PowerShell/Elvish completions generated by the CLI
- `termvox(1)` manpage generated by the CLI
- CycloneDX SBOM, SHA-256, Sigstore bundle, and GitHub provenance steps
- Homebrew/Scoop/WinGet and crates.io publication instructions are preparation
  only; no package-manager entry exists yet

## Required before beta or V1

- [x] Test Linux microphone discovery and capture.
- [ ] Test microphone capture and permission prompts on macOS and Windows.
- [ ] Record evidence for each supported route in the six-agent matrix:
      Codex, Claude Code, Cursor, Gemini CLI, Aider, and Amp.
- [x] Add reviewed Whisper base and tiny artifacts to the model manifest.
      The Spanish Vosk model entry is verified.
- [ ] Record evidence for embedded Whisper, OpenAI, Parakeet, and Vosk on every
      platform claimed in the compatibility table.
- [ ] Run a clean `install → setup → doctor → start` test for each release
      archive.
- [ ] Verify generated SBOMs, attestations, checksums, and keyless signatures.
- [ ] Confirm tag and workspace version match.
- [ ] Obtain maintainer approval before creating the remote, publishing crates
      or package-manager entries, pushing a tag, or creating a release.

An adapter existing in source is not evidence that an upstream version or
hardware path has passed these manual gates.
